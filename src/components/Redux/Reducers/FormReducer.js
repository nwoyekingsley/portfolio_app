const initialState = {
  email: "kingsleychidubem042@gmail.com",
  password: "94112"
};

const FormReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default FormReducer;
